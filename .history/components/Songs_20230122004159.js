import { useSession } from "next-auth/react"
import { useRecoilState } from "recoil"
import { playlistIdState } from "../atoms/playlistAtom"
import useSpotify from "../hooks/useSpotify"

function Songs() {
  const [ playlist, setPla]

  return (
    <div>
      <p></p>
    </div>
  )
}

export default Songs
